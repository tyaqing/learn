/*
substring() 方法返回一个字符串在开始索引到结束索引之间的一个子集
 或从开始索引直到字符串的末尾的一个子集。

 str.substring(indexStart[, indexEnd])

 返回的字符串包含start,但是不包含indexEnd
*/

let str = "sddlfa123lasdkfjiel";

const res = str.substring(1, 5);

console.log(res);
