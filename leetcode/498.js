/**
 * 498. 对角线遍历
 * @param {number[][]} matrix
 * @return {number[]}
 */
var findDiagonalOrder = function (matrix) {
  let row = (col = 0);
  let top = (left = 0);
  const right = matrix[0].length;
  const bottom = matrix.length;
  let result = [matrix[0]];
  while (1) {
    //首先右上走
    if (col - 1 < top) {
    }

    return 1;
  }
};
